# Default internal data type for constructing operators on the spherinder basis
internal_dtype = 'float128'

# Default maximum number of processes for parallel computations
max_processes = 16

# Default setting for triangular truncation of the spherinder basis functions
default_truncate = True

# Default setting for normalization of the spherinder basis functions
default_normalize = True

